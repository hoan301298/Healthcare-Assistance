const Home = () => {
    const isAuthenticated = localStorage.getItem('isAuthenticated') === 'true';
    return (
        <div className="home-page">
            <div className="intro">
                
            </div>
        </div>
    )
}

export default Home;